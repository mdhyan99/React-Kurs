const func1 = (massge) => {
    console.log(massge);
};

const func2 = (massge) => {
    console.log(massge);
};

// export const SERVER_NAME = "localhost"
const SERVER_NAME = "localhost"

// export default func1;
export  {func1,func2,SERVER_NAME};
